# Evaluation Model

Harness Forge treats evaluation as local, reproducible evidence rather than
remote analytics.

Raw events feed derived summaries so maintainers can inspect usefulness, drift,
and benchmark health from local artifacts alone.
