# Parallel Execution

Parallel execution builds on the existing flow orchestration surface instead of
creating a separate recovery silo.

Use the runtime scripts to create a plan, inspect shard status, and evaluate
merge readiness before combining work.
