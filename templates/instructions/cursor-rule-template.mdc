# Cursor Rule Template

Use this rule when Cursor needs a narrow, inspectable adaptation of the root
guidance rather than a claim of native runtime parity. Cursor should treat
`AGENTS.md`, `.agents/skills/`, `.hforge/runtime/index.json`, and the hidden
`.hforge/library/` surfaces as the bridge into the AI layer, not as evidence
of a native runtime install.
